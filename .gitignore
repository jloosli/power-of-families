.DS_Store
*~
*#
#*
node_modules
.sass-cache
.tmp

# Per-worktree env overrides (defaults live in docker-compose.yml; copy
# .env.example to .env if you need to override host ports etc.)
.env
.env.local

# Local docker-compose override (symlink to docker-compose.worktree.yml in
# worktrees to share main's DB; otherwise leave absent)
docker-compose.override.yml

db-backups

.idea

# Claude Code (local settings + worktree mirrors under .claude/worktrees/)
.claude/

# Logs
logs
*.log

db-data
wordpress
site-backup
docker/ZscalerRootCA.crt
vendor

*.cache

# Build outputs (theme dist + plugin build)
wp-content/themes/power-of-families/dist/
wp-content/plugins/pof-bloom-plugin/build/

# Test reports and coverage
coverage/
test-reports/

# xDebug logs and profiles
xdebug-logs/
xdebug-profiles/

# Smoke testing artifacts
tests/smoke/test-results/
tests/smoke/playwright-report/